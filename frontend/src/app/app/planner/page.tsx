import { FeaturePlaceholder } from '@/components/app/feature-placeholder';

export default function ContentPlannerPage() {
  return (
    <FeaturePlaceholder
      title="Content Planner"
      description="Keyword clusters (full and quick modes), filters, and create-editor actions from Surfer-style planning."
      planStep="Step 43 — /app/planner"
    />
  );
}
